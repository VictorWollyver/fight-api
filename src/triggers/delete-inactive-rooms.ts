import cron from "node-cron";
import { prisma } from "../lib/prisma";

// cron.schedule("1 * * * *", () => {
// 	try {
//     const rooms = prisma.rooms.findMany();

//   } catch (error) {

//   }
// });
